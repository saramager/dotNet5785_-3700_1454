<!--<Window x:Class="PL.ChoseForManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="ChoseForManagerWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
           
        </Grid.ColumnDefinitions>
        <Button Content="open Volunteers windows" HorizontalAlignment="Center" Height="220" Grid.Column="0" VerticalAlignment="Center" Width="220" Click="Vlountter_Click" />
        <Button Content="open manager " Height="220" Grid.Column="1"  Width="220" VerticalAlignment="Center" HorizontalAlignment="Center" Click="manager_Click"/>
      
    </Grid>
</Window>-->



<!-- הסגנונות שיצרנו קודם צריכים להיות מוגדרים כאן -->

<!-- ChoseForManagerWindow.xaml -->
<Window x:Class="PL.ChoseForManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="ChoseForManagerWindow" Height="450" Width="800"
        Background="{StaticResource cream}">
    <Grid>
       
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="8*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Label Content="Chose the window you want to enter"  Grid.Row="0" FontFamily="Cooper Black" FontSize="30"  HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource brown}" >
            <Label.Style>
                <Style/>
            </Label.Style>
        </Label>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="5*" />
                <ColumnDefinition Width="5*" />
                <ColumnDefinition Width="*" />

            </Grid.ColumnDefinitions>
     
        <Button Content="open Volunteers windows" 
                Style="{StaticResource MenuButton}"
                Height="300" 
                Width="300"
                Grid.Column="1"
                Click="Vlountter_Click" />

        <Button Content="open manager"
                Style="{StaticResource MenuButton}"
                Height="300" 
                Width="300"
                Grid.Column="2"
                Click="manager_Click" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>