<Window x:Class="PL.VolunteerScreens.VolunteerCallHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.VolunteerScreens"
        mc:Ignorable="d"
        Title="VolunteerCallHistoryWindow" Height="450" Width="800"
           DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Call History" 
                 Grid.Row="0"
                 FontSize="24" 
                 FontWeight="Bold" 
                 HorizontalAlignment="Center"
                 Margin="0,0,0,20"/>

        <!-- Filters Row -->
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="0,0,0,10">
            <Label Content="Sort By:" 
                   VerticalAlignment="Center" 
                   Margin="0,0,10,0"/>
            <ComboBox Width="150" 
                       SelectedValue="{Binding Path=sortCloseCalls, Mode=TwoWay}"   ItemsSource="{Binding Source={StaticResource FiledOFCloseListColecthionKey}}" 
                      Margin="0,0,20,0" SelectionChanged="ComboBox_SelectionChanged" />


            <Label Content="Filter:" 
                   VerticalAlignment="Center" 
                   Margin="0,0,10,0"/>
            <ComboBox Width="150"
               SelectedValue="{Binding Path=filterCloseCalls, Mode=TwoWay}" SelectionChanged="ComboBox_SelectionChanged"  ItemsSource="{Binding Source={StaticResource callTypeKey}}" 
            />
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  ItemsSource="{Binding CloseCallsList}"
                  Margin="0,10,0,0"
                >
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="*"/>
                <DataGridTextColumn Header="Address" Binding="{Binding address}" Width="*"/>
                <DataGridTextColumn Header="Call Type" Binding="{Binding callT}" Width="*"/>
                <DataGridTextColumn Header="Open Time" Binding="{Binding openTime}" Width="*"/>
                <DataGridTextColumn Header="Start Treatment" Binding="{Binding startTreatment}" Width="*"/>
                <DataGridTextColumn Header="Finish Treatment" Binding="{Binding finishTreatment}" Width="*"/>
                <DataGridTextColumn Header="Finish Type" Binding="{Binding finishT}" Width="*"/>
                <DataGridTextColumn Header="Distance" Binding="{Binding CallDistance}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Label Content="Label" HorizontalAlignment="Left" Margin="770,10,0,0" Grid.Row="2" VerticalAlignment="Top"/>
    </Grid>
</Window>
