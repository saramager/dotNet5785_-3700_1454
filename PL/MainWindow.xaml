<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="640" Width="800"
         DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Vertical" Margin="85,53,85,81" Width="230" Height="200">
            <!-- Clock value -->
            <Label Content="{Binding CurrentTime}" FontWeight="Bold" FontSize="20" Width="190" Height="30" VerticalAlignment="Center" HorizontalAlignment="Center" />
            <!-- Buttons -->
            <Button Content="Add One Minute" Click="clickAddOneMinute" HorizontalAlignment="Center" Width="190" Height="30" VerticalAlignment="Center" />
            <Button Content="Add One Hour" Click="clickAddOnehour" HorizontalAlignment="Center" Width="190" Height="30" VerticalAlignment="Center" />
            <Button Content="Add One Day" Click="clickAddOneDay" HorizontalAlignment="Center" Width="190" Height="30" VerticalAlignment="Center" />
            <Button Content="Add One Month" Click="clickAddOneMonth" HorizontalAlignment="Center" Width="190" Height="30" VerticalAlignment="Center" />
            <Button Content="Add One Year" Click="clickAddOneYear" HorizontalAlignment="Center" Width="190" Height="30" VerticalAlignment="Center" />
        </StackPanel>

        <TextBox HorizontalAlignment="Center" Height="47" Margin="0,28,0,0" TextWrapping="Wrap" Text="{Binding RiskRange, Mode=TwoWay}" VerticalAlignment="Top" Width="144" Grid.Row="1" />
        <Button Content="Update Risk Range" HorizontalAlignment="Center" Height="87" Margin="0,82,0,0" VerticalAlignment="Top" Width="144" Click="SetRiskRangeButton_Click" Grid.Row="1"/>
        <!--list of volunteers-->
        <Button Grid.Column="1" Content="Handle Volunteers" HorizontalAlignment="Left" Height="77" Margin="231,46,0,0" Grid.Row="1" VerticalAlignment="Top" Width="159" Click="ButtonVolunteerList_Click"/>
        <!--list of calls-->
        <Button Grid.Column="1" Content="Handle Calls" HorizontalAlignment="Left" Height="77" Margin="231,166,0,0" Grid.Row="1" VerticalAlignment="Top" Width="159" Click="ButtonCallList_Click"/>
        <!--resetDB-->
        <Button Content="Restet DB" HorizontalAlignment="Left" Height="78" Margin="227,209,0,0" Grid.Row="1" VerticalAlignment="Top" Width="130" Click="ButtonReset_Click"/>
        <!--Initializtion-->
        <Button Content="Initializtion" HorizontalAlignment="Left" Height="78" Margin="43,209,0,0" Grid.Row="1" VerticalAlignment="Top" Width="138" Click="Initialize_Click"/>

        <!-- Statistics Panel -->
        <GroupBox Grid.Column="1" Grid.Row="1" 
                 Header="Statistics" 
                 HorizontalAlignment="Right" 
                 VerticalAlignment="Bottom"
                 Margin="0,0,200,25"
                 Width="183" Height="242">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <!-- עמודה ראשונה -->
                    <ColumnDefinition Width="*" />
                    <!-- עמודה שנייה -->
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />


                </Grid.RowDefinitions>

                <!-- שורה 1 -->
                <Label Grid.Column="0" Content="Open calls:" HorizontalAlignment="Left" />
                <Label Grid.Column="1" Content="{Binding CallSums[0]}" HorizontalAlignment="Right" />

                <!-- שורה 2 -->
                <Label Grid.Column="0" Content="Calls in treat:" HorizontalAlignment="Left" Grid.Row="1" />
                <Label Grid.Column="1" Content="{Binding CallSums[1]}" HorizontalAlignment="Right" Grid.Row="1" />

                <!-- שורה 3 -->
                <Label Grid.Column="0" Content="Close calls:" HorizontalAlignment="Left" Grid.Row="2" />
                <Label Grid.Column="1" Content="{Binding CallSums[2]}" HorizontalAlignment="Right" Grid.Row="2" />

                <!-- שורה 4 -->
                <Label Grid.Column="0" Content="Expired calls:" HorizontalAlignment="Left" Grid.Row="3" />
                <Label Grid.Column="1" Content="{Binding CallSums[3]}" HorizontalAlignment="Right" Grid.Row="3" />

                <!-- שורה 5 -->
                <Label Grid.Column="0" Content="Open in risk calls:" HorizontalAlignment="Left" Grid.Row="4" />
                <Label Grid.Column="1" Content="{Binding CallSums[4]}" HorizontalAlignment="Right" Grid.Row="4" />

                <!-- שורה 6 -->
                <Label Grid.Column="0" Content="Treat in risk calls:" HorizontalAlignment="Left" Grid.Row="5" />
                <Label Grid.Column="1" Content="{Binding CallSums[5]}" HorizontalAlignment="Right" Grid.Row="5" />
            </Grid>


        </GroupBox>
    </Grid>
</Window>